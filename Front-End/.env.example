# Local Vite → backend on another port:
# VITE_API_BASE_URL=http://localhost:5001
#
# Production (API + UI on same Express port) — use empty so fetch goes to /api/...:
# VITE_API_BASE_URL=
VITE_API_BASE_URL=http://localhost:5001
