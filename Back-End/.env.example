# Server
PORT=5001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# JWT
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d

# MySQL
DB_NAME=finstock
DB_USER=finstock
DB_PASSWORD=
DB_HOST=217.154.54.148
DB_PORT=3306

# Email
# Default: Ethereal (free, no signup) — returns previewUrl in API responses
# Real delivery: free Resend account → set RESEND_API_KEY (https://resend.com)
EMAIL_PROVIDER=ethereal
RESEND_API_KEY=
SMTP_FROM="FinStock <onboarding@resend.dev>"

# Optional custom SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
